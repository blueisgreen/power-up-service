'use strict'
const fp = require('fastify-plugin')
const identity = require('../db/access/identity')

module.exports = fp(async function (fastify, opts) {
  // fastify.log.info('loading power up data access module')
  // fastify.register()
})
